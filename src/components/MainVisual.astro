<div class="main-visual">
  <div class="main-visual_title" data-ver="1.0.1">Basic <br />Website <br />Sample</div>
</div>

<style lang="scss">
  @use '@/styles/_dev/+' as *;

  .main-visual {
    position: relative;
    width: 100%;
    height: 100svh;
    margin-top: -100svh;
    container-type: inline-size;
    pointer-events: none;
  }

  .main-visual_title {
    position: absolute;
    bottom: rem(90);
    left: -0.04em;
    padding: 0 var(--space-frame);
    font-size: min(17vw, rem(80));
    font-weight: 900;
    line-height: 0.8;
    text-transform: uppercase;
    letter-spacing: -0.01em;

    @include min-screen(md) {
      bottom: rem(120);
    }

    &::after {
      position: absolute;
      top: rem(8);
      right: var(--space-frame);
      padding-right: rem(4);
      font-size: min(4vw, rem(16));
      font-weight: var(--typo-weight-base);
      text-transform: lowercase;
      content: 'ver. ' attr(data-ver);
    }

    @container (width > 62em) {
      font-size: 7vw;
      line-height: 1;

      br {
        display: none;
      }

      &::after {
        top: 0;
        font-size: min(1.5vw, rem(20));
        transform: translateY(-100%);
      }
    }
  }
</style>
